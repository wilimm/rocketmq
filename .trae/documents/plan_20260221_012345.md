## 方案A执行计划：I/O系统章节重组

### 修改内容

**1. 进程与线程管理章节**

* 删除 `1.6 Reactor线程模型` 整节内容

**2. I/O系统章节重组**

```
原结构 → 新结构
─────────────────────────────────────────────────
4.1 阻塞I/O → 4.1 阻塞I/O（不变）
4.2 非阻塞I/O → 4.2 非阻塞I/O（不变）
4.3 I/O多路复用 → 4.3 I/O多路复用（拆分为子章节）
  → 4.3.1 操作系统原理
  → 4.3.2 select/poll机制
  → 4.3.3 epoll机制
  → 4.3.4 触发模式详解
  → 4.3.5 RocketMQ实现
4.4 信号驱动I/O → 4.4 信号驱动I/O（不变）
4.5 异步I/O → 4.5 异步I/O（不变）
4.6 I/O模型对比与选型 → 4.6 I/O模型对比与选型（不变）
4.7 事件驱动模型 → 4.7 Reactor线程模型（合并原1.6 + 原4.7）
```

**3. 更新交叉引用**

* 更新文档开头的总概述

* 更新总结章节的表格

### 预期效果

* Reactor模型与I/O多路复用放在同一章节，逻辑更连贯

* I/O多路复用内容拆分后更易阅读

* 消除1.6与4.7的内容重叠

