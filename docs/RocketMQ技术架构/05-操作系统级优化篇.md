# RocketMQ 技术架构分析 - 操作系统级优化篇

> 本篇深入分析 RocketMQ 在操作系统层面的性能优化手段，包括 mmap 内存映射、mlock 内存锁定、堆外内存池、文件预热等核心技术。

---

## 一、优化手段总览

本节概述 RocketMQ 在操作系统层面的所有优化手段及其核心收益，为后续章节提供全局视角。

RocketMQ 通过多种操作系统级别的优化手段，实现了高吞吐、低延迟的消息存储能力：

```plain
┌─────────────────────────────────────────────────────────────────────────────┐
│                    RocketMQ 操作系统级优化架构                                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ╔═════════════════════════════════════════════════════════════════════╗   │
│  ║                         用户态 (User Space)                          ║   │
│  ║  ┌─────────────────────────────────────────────────────────────┐    ║   │
│  ║  │                     应用层 (JVM)                             │    ║   │
│  ║  │     Producer → Broker → Consumer                            │    ║   │
│  ║  └─────────────────────────────────────────────────────────────┘    ║   │
│  ║                               │                                      ║   │
│  ║                               ▼                                      ║   │
│  ║  ┌─────────────────────────────────────────────────────────────┐    ║   │
│  ║  │                     内存优化层                                │    ║   │
│  ║  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐            │    ║   │
│  ║  │  │mmap 内存映射│ │mlock 内存锁定│ │ 堆外内存池  │            │    ║   │
│  ║  │  └─────────────┘ └─────────────┘ └─────────────┘            │    ║   │
│  ║  └─────────────────────────────────────────────────────────────┘    ║   │
│  ╚═════════════════════════════════════════════════════════════════════╝   │
│  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ 系统调用边界 ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─   │
│  ╔═════════════════════════════════════════════════════════════════════╗   │
│  ║                        内核态 (Kernel Space)                         ║   │
│  ║  ┌─────────────────────────────────────────────────────────────┐    ║   │
│  ║  │                     文件系统层                                │    ║   │
│  ║  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐            │    ║   │
│  ║  │  │  文件预热   │ │ 文件预分配  │ │  顺序写入   │            │    ║   │
│  ║  │  └─────────────┘ └─────────────┘ └─────────────┘            │    ║   │
│  ║  └─────────────────────────────────────────────────────────────┘    ║   │
│  ║                               │                                      ║   │
│  ║                               ▼                                      ║   │
│  ║  ┌─────────────────────────────────────────────────────────────┐    ║   │
│  ║  │                     操作系统层                                │    ║   │
│  ║  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐            │    ║   │
│  ║  │  │  PageCache  │ │虚拟内存管理 │ │ 磁盘IO调度  │            │    ║   │
│  ║  │  └─────────────┘ └─────────────┘ └─────────────┘            │    ║   │
│  ║  └─────────────────────────────────────────────────────────────┘    ║   │
│  ╚═════════════════════════════════════════════════════════════════════╝   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.1 优化手段分类

| 优化类型 | 优化手段 | 核心收益 |
| --- | --- | --- |
| **内存映射** | mmap | 零拷贝读写，避免用户态/内核态切换 |
| **内存锁定** | mlock + madvise | 防止热点数据被 swap，提升访问速度 |
| **堆外内存** | TransientStorePool | 减少 PageCache 锁竞争，提升写入性能 |
| **文件预热** | warmMappedFile | 提前加载到 PageCache，避免运行时缺页 |
| **文件预分配** | AllocateMappedFileService | 避免文件创建阻塞写入线程 |
| **内存映射交换** | swapMap | 释放长期未访问的内存映射，降低内存压力 |

---

## 二、PageCache 核心概念

PageCache 是操作系统内核维护的磁盘数据缓存，是理解 RocketMQ 存储优化的前置知识。所有文件读写操作都经过 PageCache（除非使用 O_DIRECT），RocketMQ 的 mmap、mlock、文件预热等优化手段均围绕 PageCache 展开。

### 2.1 PageCache 概述

**PageCache** 是操作系统内核维护的磁盘数据缓存，位于用户程序与磁盘之间。所有文件读写操作都经过 PageCache（除非使用 O_DIRECT）：

| 作用 | 说明 |
|------|------|
| **读缓存** | 数据首次读取后缓存在内存，后续读取直接从内存返回 |
| **写缓存** | 写入先到 PageCache，OS 异步刷盘，减少磁盘 I/O |
| **预读** | 顺序读取时，OS 预测性地加载后续数据到 PageCache |

**关于 Buffer Cache**：在 Linux 内核 2.4 之前，PageCache（缓存文件页）和 Buffer Cache（缓存磁盘块）是分开的。从 Linux 2.4 开始，**两者统一到 Page Cache 中管理**，Buffer Cache 仅用于缓存文件系统元数据（inode、dentry 等）。对于文件 I/O 操作（read/write/mmap），主要使用 PageCache。

> **📌 重要：内核缓冲区 = PageCache（文件 I/O 场景）**
>
> 在文件 I/O 上下文中，"内核缓冲区"和"PageCache"是**同一概念**的不同称呼：
>
> | 场景 | "内核缓冲区"指代 |
> |------|------------------|
> | **文件 I/O（read/write/mmap）** | ✅ 就是 PageCache |
> | **块设备 I/O** | PageCache 中的 Buffer 部分 |
> | **网络 I/O** | Socket Buffer（不是 PageCache） |
>
> **RocketMQ 使用文件 I/O**，因此文档中提到的"内核缓冲区"均指 PageCache。理解这一点有助于消除概念混淆。

### 2.2 文件读写与 Page Cache 的关系

**所有文件读写操作都经过 Page Cache**（除非使用 O_DIRECT）：

```plain
┌─────────────────────────────────────────────────────────────────────────────┐
│                        文件读写与 Page Cache 的关系                           │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                          写入文件                                    │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │                                                                     │   │
│  │  方式一：write() 系统调用                                            │   │
│  │  ┌───────────┐      系统调用      ┌───────────┐                     │   │
│  │  │ 用户缓冲区 │ ────────────────► │ Page Cache│ ──► 异步刷盘 ──► 磁盘│   │
│  │  └───────────┘                   └───────────┘                     │   │
│  │  特点：需要数据拷贝（用户态 → 内核态）                                  │   │
│  │                                                                     │   │
│  │  方式二：mmap() 内存映射                                             │   │
│  │  ┌───────────┐      直接映射      ┌───────────┐                     │   │
│  │  │ 用户虚拟内存 │ ═══════════════► │ Page Cache│ ──► 异步刷盘 ──► 磁盘│   │
│  │  └───────────┘    (同一物理内存)   └───────────┘                     │   │
│  │  特点：零拷贝，直接写入 Page Cache                                    │   │
│  │                                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                          读取文件                                    │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │                                                                     │   │
│  │  方式一：read() 系统调用                                             │   │
│  │  ┌───────────┐      系统调用      ┌───────────┐                     │   │
│  │  │ 用户缓冲区 │ ◄──────────────── │ Page Cache│ ◄── 缓存未命中 ── 磁盘│   │
│  │  └───────────┘                   └───────────┘                     │   │
│  │  特点：缓存命中则直接返回，未命中则从磁盘加载到 Page Cache               │   │
│  │                                                                     │   │
│  │  方式二：mmap() 内存映射                                             │   │
│  │  ┌───────────┐      直接映射      ┌───────────┐                     │   │
│  │  │ 用户虚拟内存 │ ═══════════════► │ Page Cache│ ◄── 缺页中断 ── 磁盘  │   │
│  │  └───────────┘    (同一物理内存)   └───────────┘                     │   │
│  │  特点：零拷贝，缺页时触发 Page Fault 加载                             │   │
│  │                                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

**详细对比**：

| 操作方式 | 是否经过 Page Cache | 数据拷贝次数 | 说明 |
|----------|---------------------|--------------|------|
| `read()` | ✅ 是 | 1 次（内核→用户） | 从 Page Cache 拷贝到用户缓冲区 |
| `write()` | ✅ 是 | 1 次（用户→内核） | 从用户缓冲区拷贝到 Page Cache |
| `mmap()` 读 | ✅ 是 | 0 次 | 用户空间直接映射到 Page Cache |
| `mmap()` 写 | ✅ 是 | 0 次 | 用户空间直接映射到 Page Cache |
| `O_DIRECT` | ❌ 否 | 0 次 | 绕过 Page Cache，直接 I/O |

**RocketMQ 的选择**：使用 `mmap()` 内存映射方式，实现零拷贝读写 Page Cache。

**特殊情况：Direct I/O**：

使用 `O_DIRECT` 标志可以绕过 Page Cache，直接在用户缓冲区和磁盘之间传输数据。适用场景包括：
- 数据库系统（自行管理缓存）
- 大文件顺序读写（避免污染 Page Cache）
- 对数据一致性要求极高的场景

RocketMQ **不使用 Direct I/O**，因为 Page Cache 已经提供了高效的缓存，顺序写入模式下性能极佳。

### 2.3 传统 IO 数据流

```plain
┌─────────────────────────────────────────────────────────────────────────────┐
│                        传统 read/write 数据流                                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  读取流程 (磁盘 → 用户):                                                     │
│                                                                             │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                           用户态                                      │  │
│  │    ┌──────────────┐                                                  │  │
│  │    │  用户缓冲区   │  ← CPU拷贝 ←────────────────┐                    │  │
│  │    │   (JVM堆)    │                           │                     │  │
│  │    └──────────────┘                           │                     │  │
│  └───────────────────────────────────────────────┼─────────────────────┘  │
│                                     ↑ 切换       │                        │
│  ┌───────────────────────────────────────────────┼─────────────────────┐  │
│  │                           内核态               │                      │  │
│  │    ┌──────────────┐                           │                      │  │
│  │    │  PageCache   │  ─────────────────────────┘                      │  │
│  │    │ (内核缓冲区) │  ← DMA拷贝 ←───────────────┐                      │  │
│  │    │ (物理内存)   │                           │                      │  │
│  │    └──────────────┘                           │                      │  │
│  └───────────────────────────────────────────────┼─────────────────────┘  │
│                                                  │                         │
│    ┌──────────────┐                              │                         │
│    │     磁盘      │  ───────────────────────────┘                         │
│    └──────────────┘                                                       │
│                                                                            │
│  调用 read() 时发生: 用户态 ──切换──→ 内核态                                │
│  read() 返回时发生: 内核态 ──切换──→ 用户态                                 │
│                                                                            │
│  拷贝次数: DMA拷贝(磁盘→PageCache) + CPU拷贝(PageCache→用户缓冲区) = 2次    │
│                                                                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  写入流程 (用户 → 磁盘):                                                    │
│                                                                            │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                           用户态                                      │  │
│  │    ┌──────────────┐                                                  │  │
│  │    │  用户缓冲区   │  ──CPU拷贝──→                                    │  │
│  │    │   (JVM堆)    │              │                                   │  │
│  │    └──────────────┘              ↓                                   │  │
│  └──────────────────────────────────┼───────────────────────────────────┘  │
│                            ↓ 切换    │                                      │
│  ┌──────────────────────────────────┼───────────────────────────────────┐  │
│  │                           内核态  │                                   │  │
│  │    ┌──────────────┐              ↓                                   │  │
│  │    │  PageCache   │  ←────────────                                    │  │
│  │    │ (内核缓冲区) │  ──DMA拷贝──→                                    │  │
│  │    │ (物理内存)   │              │                                   │  │
│  │    └──────────────┘              ↓                                   │  │
│  └──────────────────────────────────┼───────────────────────────────────┘  │
│                                     ↓                                      │
│    ┌──────────────┐                 │                                      │
│    │     磁盘      │  ←─────────────┘                                      │
│    └──────────────┘                                                        │
│                                                                            │
│  调用 write() 时发生: 用户态 ──切换──→ 内核态                               │
│  write() 返回时发生: 内核态 ──切换──→ 用户态                                │
│                                                                            │
│  拷贝次数: CPU拷贝(用户缓冲区→PageCache) + DMA拷贝(PageCache→磁盘) = 2次    │
│                                                                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  总结:                                                                      │
│  • 读取流程: 磁盘 ─DMA→ PageCache ─CPU→ 用户缓冲区 (2次拷贝)                │
│  • 写入流程: 用户缓冲区 ─CPU→ PageCache ─DMA→ 磁盘 (2次拷贝)                │
│  • 总拷贝次数: 读取 2 次 + 写入 2 次 = 4 次                                  │
│  • 上下文切换: read() 调用+返回 = 2次，write() 调用+返回 = 2次，共 4 次      │
│                                                                            │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.4 mmap 内存映射数据流

```plain
┌─────────────────────────────────────────────────────────────────────────────┐
│                        mmap 内存映射数据流                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  映射建立 (仅一次，调用 mmap 时):                                            │
│                                                                             │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                           用户态                                      │  │
│  │    ┌──────────────────────────────────────────────────────────────┐  │  │
│  │    │                    虚拟地址空间                               │  │  │
│  │    │  ┌────────────────────────────────────────────────────────┐  │  │  │
│  │    │  │              用户态直接映射区域                          │  │  │  │
│  │    │  │         (虚拟地址 0x7f... → 物理页帧)                   │  │  │  │
│  │    │  └────────────────────────────────────────────────────────┘  │  │  │
│  │    └──────────────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                            ↑ 切换 (mmap 系统调用)                           │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                           内核态                                      │  │
│  │    ┌──────────────────────────────────────────────────────────────┐  │  │
│  │    │  建立页表映射: 虚拟地址 ←→ 物理页帧 ←→ 文件页                  │  │  │
│  │    └──────────────────────────────────────────────────────────────┘  │  │
│  │    ┌──────────────────────────────────────────────────────────────┐  │  │
│  │    │                    PageCache                                  │  │  │
│  │    │              (物理内存中的文件缓存)                             │  │  │
│  │    └──────────────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                              ↕                                              │
│    ┌──────────────────────────────────────────────────────────────────┐   │
│    │                        磁盘文件                                   │   │
│    └──────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  调用 mmap() 时发生: 用户态 ──切换──→ 内核态 (建立页表映射)                  │
│  mmap() 返回时发生: 内核态 ──切换──→ 用户态 (返回映射地址)                   │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  读取流程 (映射建立后):                                                      │
│                                                                             │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                           用户态                                      │  │
│  │    ┌──────────────────────────────────────────────────────────────┐  │  │
│  │    │  char data = mappedByteBuffer.get(index);                    │  │  │
│  │    │         ↓                                                     │  │  │
│  │    │  直接访问虚拟地址 (无系统调用)                                  │  │  │
│  │    └──────────────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                            ↓ 缺页中断 (首次访问时)                           │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                           内核态                                      │  │
│  │    ┌──────────────────────────────────────────────────────────────┐  │  │
│  │    │  缺页处理: 分配物理页 → 从磁盘加载到 PageCache → 建立映射      │  │  │
│  │    └──────────────────────────────────────────────────────────────┘  │  │
│  │    ┌──────────────────────────────────────────────────────────────┐  │  │
│  │    │                    PageCache                                  │  │  │
│  │    │              ← DMA拷贝 ← 磁盘                                 │  │  │
│  │    └──────────────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  后续访问: 直接从 PageCache 读取，无拷贝、无系统调用、无上下文切换            │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  写入流程 (映射建立后):                                                      │
│                                                                             │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                           用户态                                      │  │
│  │    ┌──────────────────────────────────────────────────────────────┐  │  │
│  │    │  mappedByteBuffer.put(index, data);                          │  │  │
│  │    │         ↓                                                     │  │  │
│  │    │  直接写入虚拟地址 (无系统调用)                                  │  │  │
│  │    └──────────────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                            ↓ 直接写入 PageCache                             │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                           内核态                                      │  │
│  │    ┌──────────────────────────────────────────────────────────────┐  │  │
│  │    │                    PageCache                                  │  │  │
│  │    │              数据已更新 (脏页)                                 │  │  │
│  │    └──────────────────────────────────────────────────────────────┘  │  │
│  │                              ↓                                      │  │
│  │    ┌──────────────────────────────────────────────────────────────┐  │  │
│  │    │  OS 后台线程异步刷盘 (pdflush/kworker)                         │  │  │
│  │    │              → DMA拷贝 → 磁盘                                 │  │  │
│  │    └──────────────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  写入完成: 用户态立即返回，OS 异步刷盘                                        │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  总结:                                                                       │
│  • 映射建立: mmap() 调用时 1 次上下文切换                                    │
│  • 读取流程: 首次缺页中断触发 DMA 拷贝，后续直接访问 PageCache (0 拷贝)       │
│  • 写入流程: 直接写入 PageCache (0 拷贝)，OS 异步刷盘 (用户无感知)            │
│  • 上下文切换: 映射后读写操作无需系统调用，0 次切换                           │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.5 DMA 拷贝说明

无论是传统 IO 还是 mmap，**磁盘与内存之间的数据传输都依赖 DMA（Direct Memory Access）**：

| 方式 | DMA 拷贝时机 | 用户感知 | 阻塞性 |
|------|-------------|----------|--------|
| **传统 IO 读取** | read() 调用时，磁盘→PageCache | 同步等待 | **阻塞**用户进程 |
| **传统 IO 写入** | write() 调用时或后台刷盘 | 可能阻塞 | 可能**阻塞** |
| **mmap 读取** | 首次访问缺页时，磁盘→PageCache | 透明处理 | **阻塞**但无需系统调用 |
| **mmap 写入** | OS 后台刷盘时，PageCache→磁盘 | 完全透明 | **不阻塞**用户进程 |

**mmap 的优势不在于消除 DMA 拷贝，而在于**：
1. **消除 CPU 拷贝**：无需在用户缓冲区和内核缓冲区之间拷贝数据
2. **消除上下文切换**：读写操作无需系统调用
3. **异步刷盘**：写入后立即返回，DMA 拷贝由 OS 后台完成

### 2.6 传统 IO 与 mmap 优劣势对比

| 维度 | 传统 IO (read/write) | mmap 内存映射 | 说明 |
|------|---------------------|---------------|------|
| **CPU 拷贝（读取）** | 1次：PageCache → 用户缓冲区 | **0 次** | mmap 直接访问 PageCache |
| **CPU 拷贝（写入）** | 1次：用户缓冲区 → PageCache | **0 次** | mmap 直接写入 PageCache |
| **CPU 拷贝（总计）** | 读取1次 + 写入1次 = 2次 | **0 次** | mmap 无需在用户/内核缓冲区间拷贝 |
| **DMA 拷贝（读取）** | 1次：磁盘 → PageCache | 1次：磁盘 → PageCache | 两者相同，磁盘IO必须经过DMA |
| **DMA 拷贝（写入）** | 1次：PageCache → 磁盘 | 1次：PageCache → 磁盘 | 两者相同，磁盘IO必须经过DMA |
| **DMA 拷贝（总计）** | 读取1次 + 写入1次 = 2次 | 读取1次 + 写入1次 = 2次 | 两者相同 |
| **上下文切换** | 4 次切换 | **0 次切换** | mmap 映射后读写无需系统调用 |
| **系统调用** | 每次 read/write 都需要 | **仅映射时需要** | mmap 映射后直接访问内存 |
| **内存占用** | 需要用户缓冲区 | **无需额外缓冲区** | mmap 直接操作 PageCache |
| **文件大小限制** | 无限制 | **受地址空间限制** | 32 位系统单次映射最大约 2GB |
| **映射开销** | 无 | **有** | mmap 初始映射需要建立页表 |
| **内存回收** | 自动 GC | **需手动管理** | MappedByteBuffer 无法被 GC 直接回收 |
| **随机访问性能** | 较差 | **优秀** | mmap 随机访问只需内存寻址 |
| **顺序访问性能** | 优秀 | 优秀 | 两者顺序访问性能相当 |
| **多进程共享** | 需要额外机制 | **天然支持** | 多进程映射同一文件可共享数据 |
| **适用场景** | 小文件、低频访问 | **大文件、高频访问** | RocketMQ 选择 mmap 处理大文件存储 |

### 2.7 RocketMQ 选择 mmap 的原因

```plain
┌─────────────────────────────────────────────────────────────────────────────┐
│                    RocketMQ 选择 mmap 的原因                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  1. 零拷贝读写                                                               │
│     ┌──────────────────────────────────────────────────────────────────┐   │
│     │  消息写入: 直接写入映射内存 → PageCache → 异步刷盘                 │   │
│     │  消息读取: 直接从映射内存读取，无需拷贝到用户缓冲区                 │   │
│     └──────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  2. 高效随机访问                                                             │
│     ┌──────────────────────────────────────────────────────────────────┐   │
│     │  ConsumeQueue 查询: 根据偏移量直接定位，O(1) 复杂度               │   │
│     │  消息索引查询: 通过哈希索引快速定位消息位置                        │   │
│     └──────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  3. 与 PageCache 深度结合                                                    │
│     ┌──────────────────────────────────────────────────────────────────┐   │
│     │  写入: 先到 PageCache，OS 异步刷盘，写入性能极高                   │   │
│     │  读取: 热点数据在 PageCache 中，读取延迟极低                       │   │
│     └──────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 三、mmap 内存映射

mmap（Memory-mapped file）将文件映射到进程的虚拟地址空间，实现**用户态直接访问文件数据**，无需系统调用，是 RocketMQ 实现高性能存储的核心技术。

### 3.1 RocketMQ 实现

源码位置 - [DefaultMappedFile.java:134-148](file:///d:/work/github/rocketmq/store/src/main/java/org/apache/rocketmq/store/logfile/DefaultMappedFile.java#L134-L148)：

```java
private void init(final String fileName, final int fileSize) throws IOException {
    this.fileName = fileName;
    this.fileSize = fileSize;
    this.file = new File(fileName);
    this.fileFromOffset = Long.parseLong(this.file.getName());  // 文件名即起始偏移量
    
    // 创建文件通道并建立内存映射
    this.fileChannel = new RandomAccessFile(this.file, "rw").getChannel();
    this.mappedByteBuffer = this.fileChannel.map(MapMode.READ_WRITE, 0, fileSize);
    
    // 统计映射内存总量和文件数量
    TOTAL_MAPPED_VIRTUAL_MEMORY.addAndGet(fileSize);
    TOTAL_MAPPED_FILES.incrementAndGet();
}
```

### 3.2 关键参数

| 参数 | 默认值 | 说明 |
| --- | --- | --- |
| `mappedFileSizeCommitLog` | 1GB (1073741824) | CommitLog 单文件大小 |
| `mappedFileSizeConsumeQueue` | 30万条 × 20字节 | ConsumeQueue 单文件大小 |
| `mappedFileSizeIndexFile` | 400MB | IndexFile 单文件大小 |

### 3.3 注意事项

| 问题 | 说明 | 解决方案 |
| --- | --- | --- |
| **文件大小限制** | 单个映射文件不宜过大 | 默认 1GB，可根据内存调整 |
| **映射开销** | mmap 操作有开销 | 预分配服务异步创建 |
| **内存占用** | 映射占用虚拟内存 | 定期 swapMap 释放不活跃映射 |

### 3.4 三种 ByteBuffer 对比

Java NIO 提供了三种 ByteBuffer 实现，RocketMQ 选择 MappedByteBuffer 作为核心存储方案：

| 特性 | HeapByteBuffer | DirectByteBuffer | MappedByteBuffer |
|------|----------------|------------------|------------------|
| **内存位置** | JVM 堆 | 堆外内存 | 堆外内存（映射文件） |
| **分配方式** | `ByteBuffer.allocate(size)` | `ByteBuffer.allocateDirect(size)` | `fileChannel.map(...)` |
| **分配/释放成本** | 低（JVM 管理） | 高（系统调用） | 高（系统调用 + 页表映射） |
| **内存大小限制** | 受 JVM 堆限制 | 受物理内存限制 | 受进程地址空间限制（~2GB/次） |
| **GC 管理** | ✅ 是 | ❌ 否（需手动释放） | ❌ 否 |
| **I/O 拷贝** | 需拷贝到临时 DirectBuffer | 直接使用 | 直接使用 |
| **与 PageCache 关系** | 隔离 | 隔离 | **共享** |
| **数据持久性** | 无（进程结束丢失） | 无（进程结束丢失） | **有**（映射到文件） |
| **多进程共享** | ❌ 否 | ❌ 否 | ✅ 是（映射同一文件） |
| **适用场景** | 短生命周期、小数据 | 高频 I/O、大数据 | **大文件读写（RocketMQ 选择）** |

**为什么 RocketMQ 选择 MappedByteBuffer？**

1. **零拷贝**：直接映射 PageCache，无需数据拷贝
2. **持久化**：数据映射到磁盘文件，进程崩溃不丢失
3. **多进程共享**：主从同步时可以共享同一文件映射
4. **大文件支持**：CommitLog 文件固定 1GB，适合映射

---

## 四、mlock 内存锁定

mlock 将进程的虚拟内存区域锁定在物理内存中，**防止被操作系统 swap 到磁盘**，保证热点数据的访问性能，常与 madvise 组合使用以优化内存访问。

### 4.1 原理概述

通过 mlock 锁定的内存区域将始终驻留物理内存，不会被操作系统换出到 Swap 分区，从而保证热点数据的访问延迟稳定。

```plain
┌─────────────────────────────────────────────────────────────────────────────┐
│                        mlock 内存锁定原理                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  未使用 mlock:                                                              │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                         物理内存 (RAM)                                │  │
│  │    ┌────────────────────────────────────────────────────────────┐   │  │
│  │    │  普通内存区域（可能被 swap 换出）                             │   │  │
│  │    └────────────────────────────────────────────────────────────┘   │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                          ↕ 可能被 swap 换入/换出                             │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                         Swap 分区 (磁盘)                              │  │
│  │    ┌────────────────────────────────────────────────────────────┐   │  │
│  │    │  被换出的内存页（访问时需要从磁盘加载）                        │   │  │
│  │    └────────────────────────────────────────────────────────────┘   │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  使用 mlock 后:                                                             │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                         物理内存 (RAM)                                │  │
│  │  ┌────────────────────────────────────────────────────────────────┐ │  │
│  │  │  CommitLog 热点文件 (mlock 锁定)                                 │ │  │
│  │  │  ╔══════════════════════════════════════════════════════════╗ │ │  │
│  │  │  ║  • 始终驻留物理内存，不会被 swap 换出                      ║ │ │  │
│  │  │  ║  • 访问延迟稳定，无磁盘 I/O                                ║ │ │  │
│  │  │  ╚══════════════════════════════════════════════════════════╝ │ │  │
│  │  └────────────────────────────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  Swap 分区: mlock 锁定的内存不会被换出到此处                                 │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 4.2 RocketMQ 实现

源码位置 - [LibC.java:42](file:///d:/work/github/rocketmq/store/src/main/java/org/apache/rocketmq/store/util/LibC.java#L42)：

```java
public interface LibC extends Library {
    LibC INSTANCE = (LibC) Native.loadLibrary(
        Platform.isWindows() ? "msvcrt" : "c", LibC.class);
    
    int mlock(Pointer var1, NativeLong var2);    // 锁定内存
    int munlock(Pointer var1, NativeLong var2);  // 解锁内存
    int madvise(Pointer var1, NativeLong var2, int var3);  // 内存访问建议
}
```

源码位置 - [DefaultMappedFile.java:667-684](file:///d:/work/github/rocketmq/store/src/main/java/org/apache/rocketmq/store/logfile/DefaultMappedFile.java#L667-L684)：

```java
public void mlock() {
    final long beginTime = System.currentTimeMillis();
    final long address = ((DirectBuffer) (this.mappedByteBuffer)).address();
    Pointer pointer = new Pointer(address);
    
    // 锁定内存，防止被 swap
    int ret = LibC.INSTANCE.mlock(pointer, new NativeLong(this.fileSize));
    
    // 建议内核预加载该区域
    int ret = LibC.INSTANCE.madvise(pointer, new NativeLong(this.fileSize), 
                                    LibC.MADV_WILLNEED);
}
```

### 4.3 系统调用详解

mlock 和 madvise 都是 Linux/Unix 操作系统提供的**系统调用**函数：

#### 4.3.1 mlock 系统调用

| 属性 | 说明 |
| --- | --- |
| **头文件** | `#include <sys/mman.h>` |
| **函数签名** | `int mlock(const void *addr, size_t len)` |
| **功能** | 将指定内存区域锁定在物理内存中，防止被 swap 到磁盘 |
| **返回值** | 成功返回 0，失败返回 -1 |
| **权限要求** | 需要 `CAP_IPC_LOCK` 权限或 root 权限 |

相关系统调用：

```c
// 锁定指定内存区域
int mlock(const void *addr, size_t len);

// 锁定进程的所有内存
int mlockall(int flags);

// 解锁内存
int munlock(const void *addr, size_t len);
int munlockall(void);
```

#### 4.3.2 madvise 系统调用

| 属性 | 说明 |
| --- | --- |
| **头文件** | `#include <sys/mman.h>` |
| **函数签名** | `int madvise(void *addr, size_t length, int advice)` |
| **功能** | 向内核提供内存区域的使用建议，让内核优化内存管理策略 |
| **返回值** | 成功返回 0，失败返回 -1 |
| **权限要求** | 无特殊权限要求 |

常用 advice 参数：

```c
// 预加载建议
MADV_WILLNEED    // 告诉内核这部分内存即将被访问，预加载到 PageCache

// 释放建议
MADV_DONTNEED    // 告诉内核这部分内存不再需要，可以释放
MADV_FREE        // 标记内存为可释放（延迟释放，访问时自动恢复）

// 访问模式建议
MADV_SEQUENTIAL  // 顺序访问模式，内核会激进预读
MADV_RANDOM      // 随机访问模式，内核减少预读
MADV_NORMAL      // 默认访问模式
```

#### 4.3.3 mlock vs madvise 对比

| 特性 | mlock | madvise |
| --- | --- | --- |
| **强制性** | 强制锁定，内核必须遵守 | 建议性，内核可以选择忽略 |
| **权限要求** | 需要 `CAP_IPC_LOCK` 或 root | 无特殊权限要求 |
| **内存效果** | 内存不会被 swap | 内存可能被 swap，但内核会优化访问 |
| **典型场景** | 关键热点数据，必须常驻内存 | 预加载、访问模式优化 |

#### 4.3.4 RocketMQ 组合策略

RocketMQ 组合使用 mlock 和 madvise 实现热点数据优化：

1. **mlock** - 确保热点 CommitLog 数据**绝对不会**被 swap
2. **madvise(MADV_WILLNEED)** - 提前触发内核预读，减少首次访问延迟

这种组合策略在消息队列场景中非常有效，因为消息存储对延迟敏感，且访问模式通常有明显的热点区域。

### 4.4 使用场景

| 场景 | 是否启用 | 原因 |
| --- | --- | --- |
| 高吞吐写入 | ✅ 启用 | 热点文件常驻内存，避免缺页中断 |
| 内存紧张 | ⚠️ 谨慎 | 锁定内存会减少可用内存 |
| 多实例部署 | ⚠️ 谨慎 | 需要合理规划锁定内存总量 |

---

## 五、堆外内存池 (TransientStorePool)

TransientStorePool 是 RocketMQ 提供的**堆外内存池**，用于优化异步刷盘场景的写入性能，通过减少 PageCache 锁竞争实现更高吞吐。

### 5.1 原理概述

写入时先写入堆外内存池，再由后台线程异步提交到 PageCache，从而减少多线程写入时的 PageCache 锁竞争，提升写入吞吐量。

```plain
┌─────────────────────────────────────────────────────────────────────────────┐
│                    TransientStorePool 架构                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ╔═════════════════════════════════════════════════════════════════════╗   │
│  ║                       用户态 (User Space)                            ║   │
│  ║                                                                     ║   │
│  ║  ┌─────────────────────────────────────────────────────────────┐   ║   │
│  ║  │                   TransientStorePool                         │   ║   │
│  ║  │  ┌─────────────────────────────────────────────────────┐    │   ║   │
│  ║  │  │ availableBuffers (ConcurrentLinkedDeque<ByteBuffer>)│    │   ║   │
│  ║  │  │  ┌─────────┐┌─────────┐┌─────────┐┌─────────┐┌────┐│    │   ║   │
│  ║  │  │  │Buffer 1 ││Buffer 2 ││Buffer 3 ││Buffer 4 ││... ││    │   ║   │
│  ║  │  │  │ (1GB)   ││ (1GB)   ││ (1GB)   ││ (1GB)   ││    ││    │   ║   │
│  ║  │  │  │ mlock   ││ mlock   ││ mlock   ││ mlock   ││    ││    │   ║   │
│  ║  │  │  └─────────┘└─────────┘└─────────┘└─────────┘└────┘│    │   ║   │
│  ║  │  └─────────────────────────────────────────────────────┘    │   ║   │
│  ║  │                                                             │   ║   │
│  ║  │  总内存占用: poolSize × fileSize = 5 × 1GB = 5GB            │   ║   │
│  ║  └─────────────────────────────────────────────────────────────┘   ║   │
│  ╚═════════════════════════════════════════════════════════════════════╝   │
│  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ 系统调用边界 ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─   │
│  ╔═════════════════════════════════════════════════════════════════════╗   │
│  ║                       内核态 (Kernel Space)                          ║   │
│  ║                                                                     ║   │
│  ║  ┌─────────────────────────────────────────────────────────────┐   ║   │
│  ║  │                     PageCache                                │   ║   │
│  ║  │  • commit 后数据从 writeBuffer 写入此处                      │   ║   │
│  ║  │  • flush 后数据从 PageCache 刷入磁盘                         │   ║   │
│  ║  └─────────────────────────────────────────────────────────────┘   ║   │
│  ╚═════════════════════════════════════════════════════════════════════╝   │
│                                                                             │
│  数据流转:                                                                   │
│  Producer → writeBuffer (用户态堆外内存) → commit → PageCache → flush → 磁盘│
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 5.2 核心实现

TransientStorePool 是 RocketMQ 提供的堆外内存池，用于优化异步刷盘场景的写入性能。

源码位置 - [TransientStorePool.java:39-59](file:///d:/work/github/rocketmq/store/src/main/java/org/apache/rocketmq/store/TransientStorePool.java#L39-L59)：

```java
public class TransientStorePool {
    private final int poolSize;                       // 池大小，默认 5
    private final int fileSize;                       // 每个缓冲区大小，默认 1GB
    private final Deque<ByteBuffer> availableBuffers; // 可用缓冲区队列
    
    public void init() {
        for (int i = 0; i < poolSize; i++) {
            // 分配堆外内存
            ByteBuffer byteBuffer = ByteBuffer.allocateDirect(fileSize);
            
            // 获取内存地址并锁定，防止被 swap
            final long address = ((DirectBuffer) byteBuffer).address();
            Pointer pointer = new Pointer(address);
            LibC.INSTANCE.mlock(pointer, new NativeLong(fileSize));
            
            availableBuffers.offer(byteBuffer);  // 加入可用队列
        }
    }
}
```

### 5.3 配置参数

源码位置 - [MessageStoreConfig.java:211-212](file:///d:/work/github/rocketmq/store/src/main/java/org/apache/rocketmq/store/config/MessageStoreConfig.java#L211-L212)：

| 参数 | 默认值 | 说明 |
| --- | --- | --- |
| `transientStorePoolEnable` | false | 是否启用堆外内存池 |
| `transientStorePoolSize` | 5 | 内存池中 Buffer 数量，每个 Buffer 大小为 `mappedFileSizeCommitLog`（默认 1GB） |

### 5.4 启用条件

```java
public boolean isTransientStorePoolEnable() {
    return transientStorePoolEnable && BrokerRole.SLAVE != getBrokerRole();
}
```

两者缺一不可：
- `transientStorePoolEnable = true`
- `brokerRole ≠ SLAVE`（非从节点）

> **注意**：TransientStorePool 主要用于异步刷盘场景（ASYNC_FLUSH）。同步刷盘时，数据需要立即写入 PageCache，使用堆外内存池反而会增加一次内存拷贝。

### 5.5 与默认模式对比

| 维度 | MappedByteBuffer（默认） | TransientStorePool |
| --- | --- | --- |
| **写入路径** | 直接写入 PageCache | 先写入 writeBuffer，再 commit 到 PageCache |
| **写入性能** | 较高 | **更高**（减少 PageCache 锁竞争） |
| **数据安全性** | **较高**（PageCache 可恢复） | 较低（writeBuffer 数据易丢失） |
| **内存占用** | PageCache | PageCache + 堆外内存池（额外 5GB） |

### 5.6 数据流转

```plain
启用 TransientStorePool 后的写入流程：

Producer
    │
    ▼
╔═══════════════════════════════════════════════════════════════════════════╗
║                           用户态 (User Space)                              ║
║                                                                           ║
║  writeBuffer (堆外内存池 TransientStorePool)                               ║
║  ┌─────────────────────────────────────────────────────────────────────┐ ║
║  │  Msg1 │ Msg2 │ Msg3 │ Msg4 │ ...                                    │ ║
║  └─────────────────────────────────────────────────────────────────────┘ ║
║  wrotePosition: 写入位置                                                   ║
║                                                                           ║
╚═══════════════════════════════════════════════════════════════════════════╝
    │
    │ commit (异步提交线程，系统调用 write)
    ▼
─────────────────────────────────────────────────────────────────────────────
                           系统调用边界
─────────────────────────────────────────────────────────────────────────────
    │
    ▼
╔═══════════════════════════════════════════════════════════════════════════╗
║                          内核态 (Kernel Space)                             ║
║                                                                           ║
║  PageCache (MappedByteBuffer)                                             ║
║  ┌─────────────────────────────────────────────────────────────────────┐ ║
║  │  Msg1 │ Msg2 │ Msg3 │ Msg4 │ ...                                    │ ║
║  └─────────────────────────────────────────────────────────────────────┘ ║
║  committedPosition: 提交位置                                               ║
║                                                                           ║
╚═══════════════════════════════════════════════════════════════════════════╝
    │
    │ flush (异步刷盘线程，系统调用 fsync)
    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  磁盘 (CommitLog 文件)                                                       │
│  flushedPosition: 刷盘位置                                                   │
└─────────────────────────────────────────────────────────────────────────────┘

位置关系：wrotePosition ≥ committedPosition ≥ flushedPosition
```

### 5.7 适用场景

| 场景 | 建议 | 原因 |
| --- | --- | --- |
| 金融交易、订单系统 | ❌ 不启用 | 数据安全优先 |
| 日志采集、消息推送 | ✅ 启用 | 可容忍少量丢失，追求高吞吐 |

---

## 六、文件预热 (warmMappedFile)

文件预热通过**预先写入数据**将文件内容加载到 PageCache，避免运行时缺页中断，常与 mlock 配合确保热点数据常驻内存。

### 6.1 原理概述

通过逐页写入 0 的方式将文件内容提前加载到 PageCache，避免首次访问时的缺页中断和磁盘读取延迟。

```plain
┌─────────────────────────────────────────────────────────────────────────────┐
│                        文件预热效果                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  未预热:                                                                    │
│  ╔═════════════════════════════════════════════════════════════════════╗   │
│  ║                       用户态 (User Space)                            ║   │
│  ║  时间线: T1 ─────────────────────────────────────────────────→      ║   │
│  ║          │                                                          ║   │
│  ║          ▼ 首次访问触发缺页中断                                      ║   │
│  ║  ┌──────────────────────────────────────────────────────────────┐  ║   │
│  ║  │  访问位置: [Page 0] [Page 1] [Page 2] ... [Page N]            │  ║   │
│  ║  │            ↓ 缺页    ↓ 缺页    ↓ 缺页                         │  ║   │
│  ║  │  延迟:    高       高       高                                 │  ║   │
│  ║  └──────────────────────────────────────────────────────────────┘  ║   │
│  ╚═════════════════════════════════════════════════════════════════════╝   │
│                          ↓ 缺页中断触发磁盘读取                              │
│  ╔═════════════════════════════════════════════════════════════════════╗   │
│  ║                       内核态 (Kernel Space)                          ║   │
│  ║  PageCache: 按需加载，每次访问都需要从磁盘读取                         ║   │
│  ╚═════════════════════════════════════════════════════════════════════╝   │
│                                                                             │
│  预热后:                                                                    │
│  ╔═════════════════════════════════════════════════════════════════════╗   │
│  ║                       用户态 (User Space)                            ║   │
│  ║  时间线: T0(预热) ─────────────────────────────────────────→        ║   │
│  ║          │                                                          ║   │
│  ║          ▼ 运行时访问: 无缺页中断，延迟稳定                            ║   │
│  ║  ┌──────────────────────────────────────────────────────────────┐  ║   │
│  ║  │  MappedByteBuffer: 直接访问已映射的内存                        │  ║   │
│  ║  └──────────────────────────────────────────────────────────────┘  ║   │
│  ╚═════════════════════════════════════════════════════════════════════╝   │
│  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ mmap 映射边界 ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─   │
│  ╔═════════════════════════════════════════════════════════════════════╗   │
│  ║                       内核态 (Kernel Space)                          ║   │
│  ║  ┌──────────────────────────────────────────────────────────────┐  ║   │
│  ║  │  PageCache: [Page 0] [Page 1] [Page 2] ... [Page N]           │  ║   │
│  ║  │              ← 预热时全部加载到内存                             │  ║   │
│  ║  └──────────────────────────────────────────────────────────────┘  ║   │
│  ║                                                                     ║   │
│  ║  mlock 后: PageCache 被锁定，不会被 swap 换出                        ║   │
│  ╚═════════════════════════════════════════════════════════════════════╝   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 6.2 RocketMQ 实现

文件预热通过按 OS_PAGE_SIZE (4KB) 逐页写入 0，将文件内容加载到 PageCache，预热完成后执行 mlock 锁定内存。

源码位置 - [DefaultMappedFile.java:533-580](file:///d:/work/github/rocketmq/store/src/main/java/org/apache/rocketmq/store/logfile/DefaultMappedFile.java#L533-L580)：

```java
public void warmMappedFile(FlushDiskType type, int pages) {
    long beginTime = System.currentTimeMillis();
    ByteBuffer byteBuffer = this.mappedByteBuffer.slice();
    int flush = 0;
    
    // 按 OS_PAGE_SIZE (4KB) 逐页写入 0，触发缺页加载到 PageCache
    for (int i = 0, j = 0; i < this.fileSize; i += OS_PAGE_SIZE, j++) {
        byteBuffer.put(i, (byte) 0);
        
        // 同步刷盘模式下，定期强制刷盘
        if (type == FlushDiskType.SYNC_FLUSH) {
            if ((i / OS_PAGE_SIZE) - (flush / OS_PAGE_SIZE) >= pages) {
                flush = i;
                mappedByteBuffer.force();
            }
        }
        
        // 防止 GC，每 1000 次让出 CPU
        if (j % 1000 == 0) {
            Thread.sleep(0);
        }
    }
    
    // 预热完成后执行 mlock，锁定内存防止被 swap
    this.mlock();
}
```

### 6.3 配置参数

源码位置 - [MessageStoreConfig.java:155,202](file:///d:/work/github/rocketmq/store/src/main/java/org/apache/rocketmq/store/config/MessageStoreConfig.java#L155)：

| 参数 | 默认值 | 说明 |
| --- | --- | --- |
| `warmMapedFileEnable` | false | 是否启用文件预热 |
| `flushLeastPagesWhenWarmMapedFile` | 4096 页（约 16MB） | 预热时每次刷盘的页数，每页 4KB |

### 6.4 预热时机

文件预热在 `AllocateMappedFileService` 创建新文件时触发：

```java
if (mappedFile.getFileSize() >= mappedFileSizeCommitLog 
    && warmMapedFileEnable) {
    mappedFile.warmMappedFile(flushDiskType, flushLeastPagesWhenWarmMapedFile);
}
```

---

## 七、文件预分配 (AllocateMappedFileService)

文件预分配服务通过**异步创建 MappedFile** 避免文件创建阻塞消息写入线程，是 RocketMQ 实现高吞吐写入的关键机制。

### 7.1 原理概述

后台线程提前创建下一个 MappedFile，当当前文件写满时，写入线程可以无阻塞地获取预分配好的文件继续写入。

```plain
┌─────────────────────────────────────────────────────────────────────────────┐
│                    文件预分配流程                                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ╔═════════════════════════════════════════════════════════════════════╗   │
│  ║                       用户态 (User Space)                            ║   │
│  ║                                                                     ║   │
│  ║  写入线程 (主线程):                                                  ║   │
│  ║  ┌──────────────────────────────────────────────────────────────┐  ║   │
│  ║  │  T1: 写入 Msg1 → MappedFile[0] (已存在)                       │  ║   │
│  ║  │  T2: 写入 Msg2 → MappedFile[0] (已存在)                       │  ║   │
│  ║  │  T3: 写入 Msg3 → MappedFile[0] (已存在)                       │  ║   │
│  ║  │  ...                                                          │  ║   │
│  ║  │  TN: MappedFile[0] 快满 → 请求下一个文件                       │  ║   │
│  ║  │       ↓                                                       │  ║   │
│  ║  │       ↓ 从 requestTable 获取预分配的文件 (无阻塞)              │  ║   │
│  ║  │       ↓                                                       │  ║   │
│  ║  │  TN+1: 写入 MsgN → MappedFile[1] (已预分配完成)               │  ║   │
│  ║  └──────────────────────────────────────────────────────────────┘  ║   │
│  ║                                                                     ║   │
│  ║  预分配线程 (AllocateMappedFileService):                             ║   │
│  ║  ┌──────────────────────────────────────────────────────────────┐  ║   │
│  ║  │  持续运行，监听 requestQueue                                  │  ║   │
│  ║  │                                                               │  ║   │
│  ║  │  while (!stopped) {                                           │  ║   │
│  ║  │      AllocateRequest req = requestQueue.take();               │  ║   │
│  ║  │      MappedFile mappedFile = new DefaultMappedFile(...);      │  ║   │
│  ║  │      mappedFile.warmMappedFile(...);  // 可选预热              │  ║   │
│  ║  │      req.setMappedFile(mappedFile);                           │  ║   │
│  ║  │      req.getCountDownLatch().countDown();                     │  ║   │
│  ║  │  }                                                            │  ║   │
│  ║  └──────────────────────────────────────────────────────────────┘  ║   │
│  ╚═════════════════════════════════════════════════════════════════════╝   │
│  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ 系统调用边界 ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─   │
│  ╔═════════════════════════════════════════════════════════════════════╗   │
│  ║                       内核态 (Kernel Space)                          ║   │
│  ║                                                                     ║   │
│  ║  文件创建与 mmap 映射 (由预分配线程触发):                              ║   │
│  ║  ┌──────────────────────────────────────────────────────────────┐  ║   │
│  ║  │  1. open() - 创建新文件                                       │  ║   │
│  ║  │  2. mmap() - 建立内存映射                                     │  ║   │
│  ║  │  3. 预热时: 写入 PageCache (触发缺页加载)                       │  ║   │
│  ║  │  4. mlock() - 锁定内存 (可选)                                  │  ║   │
│  ║  └──────────────────────────────────────────────────────────────┘  ║   │
│  ╚═════════════════════════════════════════════════════════════════════╝   │
│                                                                             │
│  优势: 文件创建、mmap 映射、预热都在后台线程完成，不阻塞写入                  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 7.2 核心实现

文件预分配服务通过后台线程异步创建 MappedFile，避免文件创建阻塞消息写入线程。

源码位置 - [AllocateMappedFileService.java:153-218](file:///d:/work/github/rocketmq/store/src/main/java/org/apache/rocketmq/store/AllocateMappedFileService.java#L153-L218)：

```java
private boolean mmapOperation() {
    // 从队列获取预分配请求
    AllocateRequest req = this.requestQueue.take();
    
    if (req.getMappedFile() == null) {
        long beginTime = System.currentTimeMillis();
        
        MappedFile mappedFile;
        // 根据配置选择是否使用堆外内存池
        if (messageStore.getMessageStoreConfig().isTransientStorePoolEnable()) {
            mappedFile = new DefaultMappedFile(req.getFilePath(), 
                req.getFileSize(), messageStore.getTransientStorePool());
        } else {
            mappedFile = new DefaultMappedFile(req.getFilePath(), req.getFileSize());
        }
        
        // 文件预热（如果启用）
        if (mappedFile.getFileSize() >= mappedFileSizeCommitLog 
            && warmMapedFileEnable) {
            mappedFile.warmMappedFile(flushDiskType, flushLeastPagesWhenWarmMapedFile);
        }
        
        req.setMappedFile(mappedFile);  // 设置结果
        req.getCountDownLatch().countDown();  // 通知等待线程
    }
    return true;
}
```

### 7.3 关键设计

| 设计点 | 说明 |
| --- | --- |
| **异步创建** | 后台线程循环处理请求，不阻塞写入 |
| **优先级队列** | 按文件大小和名称排序，保证顺序 |
| **超时机制** | 等待超时后直接创建，保证可用性 |
| **异常恢复** | 异常时重新入队，保证不丢失请求 |

---

## 八、内存映射交换 (swapMap)

swapMap 用于**释放长期未访问的内存映射**，降低内存压力，适用于长期运行的 Broker 实例。

### 8.1 原理概述

通过重新映射文件获得新的虚拟地址，释放旧的虚拟内存映射，从而降低进程的虚拟内存占用。

```plain
┌─────────────────────────────────────────────────────────────────────────────┐
│                        swapMap 工作原理                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ╔═════════════════════════════════════════════════════════════════════╗   │
│  ║                       用户态 (User Space)                            ║   │
│  ║                                                                     ║   │
│  ║  内存映射文件队列 (MappedFile 列表):                                  ║   │
│  ║  ┌──────────────────────────────────────────────────────────────┐  ║   │
│  ║  │  [File 0] [File 1] [File 2] [File 3] [File 4] [File 5] [...] │  ║   │
│  ║  │    ↑       ↑       ↑       ↑       ↑       ↑                 │  ║   │
│  ║  │   旧      旧      旧      旧      热点    写入中              │  ║   │
│  ║  │    ↓       ↓       ↓       ↓                                 │  ║   │
│  ║  │  swapMap swapMap swapMap swapMap                             │  ║   │
│  ║  │    ↓       ↓       ↓       ↓                                 │  ║   │
│  ║  │  释放旧映射，重新映射到新地址                                  │  ║   │
│  ║  └──────────────────────────────────────────────────────────────┘  ║   │
│  ║                                                                     ║   │
│  ║  swapMap 触发条件:                                                  ║   │
│  ║  1. 强制交换: 距上次交换时间 > forceSwapIntervalMs                   ║   │
│  ║  2. 正常交换: 距上次交换时间 > normalSwapIntervalMs 且有访问         ║   │
│  ║                                                                     ║   │
│  ║  保留策略: 保留最近 3 个文件不交换（正在写入和热点文件）               ║   │
│  ╚═════════════════════════════════════════════════════════════════════╝   │
│  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ 系统调用边界 ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─   │
│  ╔═════════════════════════════════════════════════════════════════════╗   │
│  ║                       内核态 (Kernel Space)                          ║   │
│  ║                                                                     ║   │
│  ║  swapMap 执行流程:                                                  ║   │
│  ║  ┌──────────────────────────────────────────────────────────────┐  ║   │
│  ║  │  1. 保存旧的 mappedByteBuffer 引用                             │  ║   │
│  ║  │  2. 调用 mmap() 重新映射文件，获得新虚拟地址                    │  ║   │
│  ║  │  3. 旧映射等待 cleanSwapedMap() 清理                           │  ║   │
│  ║  │  4. 清理时调用 munmap() 释放旧虚拟内存                          │  ║   │
│  ║  └──────────────────────────────────────────────────────────────┘  ║   │
│  ║                                                                     ║   │
│  ║  效果: 释放长期未访问的虚拟内存，降低进程虚拟内存占用                  ║   │
│  ╚═════════════════════════════════════════════════════════════════════╝   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 8.2 核心实现

swapMap 用于释放长期未访问的内存映射，通过重新映射释放旧的虚拟内存，降低内存压力。

源码位置 - [DefaultMappedFile.java:575-600](file:///d:/work/github/rocketmq/store/src/main/java/org/apache/rocketmq/store/logfile/DefaultMappedFile.java#L575-L600)：

```java
public boolean swapMap() {
    if (getRefCount() == 1 && this.mappedByteBufferWaitToClean == null) {
        if (!hold()) {
            return false;
        }
        try {
            // 保存旧映射，等待清理
            this.mappedByteBufferWaitToClean = this.mappedByteBuffer;
            
            // 重新映射，获得新的虚拟地址
            this.mappedByteBuffer = this.fileChannel.map(
                FileChannel.MapMode.READ_WRITE, 0, fileSize);
            this.mappedByteBufferAccessCountSinceLastSwap = 0L;
            this.swapMapTime = System.currentTimeMillis();
            return true;
        } finally {
            this.release();
        }
    }
    return false;
}
```

### 8.3 清理旧映射

源码位置 - [DefaultMappedFile.java:603-622](file:///d:/work/github/rocketmq/store/src/main/java/org/apache/rocketmq/store/logfile/DefaultMappedFile.java#L603-L622)：

```java
public void cleanSwapedMap(boolean force) {
    if (this.mappedByteBufferWaitToClean == null) {
        return;
    }
    
    long minGapTime = 120 * 1000L;  // 最小间隔 120s (2分钟)
    long gapTime = System.currentTimeMillis() - this.swapMapTime;
    
    if (!force && gapTime < minGapTime) {
        Thread.sleep(minGapTime - gapTime);  // 等待达到最小间隔
    }
    
    // 清理旧的映射
    UtilAll.cleanBuffer(this.mappedByteBufferWaitToClean);
    this.mappedByteBufferWaitToClean = null;
}
```

### 8.4 配置参数

源码位置 - [MessageStoreConfig.java:249-255](file:///d:/work/github/rocketmq/store/src/main/java/org/apache/rocketmq/store/config/MessageStoreConfig.java#L249-L255)：

| 参数 | 默认值 | 说明 |
| --- | --- | --- |
| `mappedFileSwapEnable` | true | 是否启用内存映射交换 |
| `commitLogForceSwapMapInterval` | 12 小时（43200000ms） | 强制交换间隔 |
| `commitLogSwapMapInterval` | 1 小时（3600000ms） | 正常交换间隔 |
| `commitLogSwapMapReserveFileNum` | 100 | 保留的映射文件数量 |
| `cleanSwapedMapInterval` | 5 分钟（300000ms） | 清理旧映射的时间间隔 |

---

## 九、优化手段对比总结

本节对 RocketMQ 的各项操作系统级优化手段进行横向对比，帮助读者根据实际场景选择合适的配置组合。

### 9.1 性能影响对比

| 优化手段 | 写入性能 | 读取性能 | 内存占用 | 数据安全 | 适用场景 |
| --- | --- | --- | --- | --- | --- |
| **mmap** | ⬆️ 高 | ⬆️ 高 | 中 | 中 | 所有场景 |
| **mlock** | - | ⬆️ 高 | ⬆️ 高 | ⬆️ 高 | 热点数据 |
| **TransientStorePool** | ⬆️⬆️ 很高 | - | ⬆️⬆️ 很高 | ⬇️ 低 | 异步刷盘、高吞吐 |
| **文件预热** | - | ⬆️ 高 | ⬆️ 高 | - | 启动加速 |
| **文件预分配** | ⬆️ 高 | - | - | - | 所有场景 |
| **swapMap** | - | - | ⬇️ 低 | - | 长期运行 |

### 9.2 配置建议

| 场景 | 推荐配置 | 说明 |
| --- | --- | --- |
| **金融交易** | mmap + mlock + 预分配 | 数据安全优先 |
| **日志采集** | TransientStorePool + 预热 | 吞吐量优先 |
| **混合场景** | mmap + 预分配 + swapMap | 平衡性能与资源 |
| **内存紧张** | mmap + swapMap | 释放不活跃内存 |

### 9.3 关键源码索引

| 优化手段 | 核心类 | 关键方法 |
| --- | --- | --- |
| mmap | [DefaultMappedFile.java](file:///d:/work/github/rocketmq/store/src/main/java/org/apache/rocketmq/store/logfile/DefaultMappedFile.java) | `init()` |
| mlock | [DefaultMappedFile.java](file:///d:/work/github/rocketmq/store/src/main/java/org/apache/rocketmq/store/logfile/DefaultMappedFile.java) | `mlock()` |
| madvise | [LibC.java](file:///d:/work/github/rocketmq/store/src/main/java/org/apache/rocketmq/store/util/LibC.java) | `madvise()` |
| TransientStorePool | [TransientStorePool.java](file:///d:/work/github/rocketmq/store/src/main/java/org/apache/rocketmq/store/TransientStorePool.java) | `init()` |
| 文件预热 | [DefaultMappedFile.java](file:///d:/work/github/rocketmq/store/src/main/java/org/apache/rocketmq/store/logfile/DefaultMappedFile.java) | `warmMappedFile()` |
| 文件预分配 | [AllocateMappedFileService.java](file:///d:/work/github/rocketmq/store/src/main/java/org/apache/rocketmq/store/AllocateMappedFileService.java) | `mmapOperation()` |
| swapMap | [DefaultMappedFile.java](file:///d:/work/github/rocketmq/store/src/main/java/org/apache/rocketmq/store/logfile/DefaultMappedFile.java) | `swapMap()` |

---

## 十、最佳实践

本节提供生产环境的推荐配置、JVM 参数和操作系统参数建议，帮助读者快速部署高性能 RocketMQ 集群。

### 10.1 生产环境推荐配置

```properties
# 内存映射配置
mappedFileSizeCommitLog=1073741824

# 堆外内存池（异步刷盘场景）
transientStorePoolEnable=true
transientStorePoolSize=5

# 文件预热
warmMapedFileEnable=true
flushLeastPagesWhenWarmMapedFile=4096

# 内存映射交换
cleanSwapedMapInterval=300000
```

### 10.2 JVM 参数建议

```bash
# 堆外内存限制（需要容纳 TransientStorePool）
-XX:MaxDirectMemorySize=8g

# 大页内存（可选，提升 mmap 性能）
-XX:+UseLargePages
```

### 10.3 操作系统参数建议

```bash
# 增加最大文件映射数量
echo 262144 > /proc/sys/vm/max_map_count

# 减少 swap 使用倾向
echo 10 > /proc/sys/vm/swappiness

# 禁用 THP（透明大页），避免内存碎片
echo never > /sys/kernel/mm/transparent_hugepage/enabled
```

---

## 十一、总结

本篇深入分析了 RocketMQ 在操作系统层面的性能优化手段：

| 优化手段 | 核心作用 | 关键技术 |
| --- | --- | --- |
| **mmap 内存映射** | 零拷贝读写 | FileChannel.map() |
| **mlock 内存锁定** | 防止热点数据被 swap | mlock + madvise |
| **堆外内存池** | 减少锁竞争，提升写入性能 | TransientStorePool |
| **文件预热** | 提前加载到 PageCache | warmMappedFile |
| **文件预分配** | 避免文件创建阻塞写入 | AllocateMappedFileService |
| **内存映射交换** | 释放不活跃内存 | swapMap |

这些优化手段相互配合，共同支撑 RocketMQ 的高吞吐、低延迟存储能力。在实际应用中，需要根据业务场景（数据安全性 vs 吞吐量）选择合适的配置组合。
